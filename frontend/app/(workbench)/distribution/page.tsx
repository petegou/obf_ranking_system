import { ComingSoonPanel } from "@/components/placeholders/coming-soon-panel";

export default function DistributionPage() {
  return (
    <ComingSoonPanel
      title="Score Distribution"
      description="Histograms and box plots showing how scores are distributed within each category. Use it to inspect outliers, medians, quartiles, and categories where scoring is concentrated or widely dispersed."
    />
  );
}
